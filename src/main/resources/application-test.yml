spring:
  datasource:
    url: jdbc:postgresql://localhost:5555/ProductService
    username: postgres
    password: '@admin123'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none

